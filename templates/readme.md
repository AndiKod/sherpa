#sh:erpa Templates

A collection of newFile templates, to be used as model when creating new pages from the command line. Files in this folder are examples that can be copied to the $dotDir to be used.

The real templates, listed with `s l-tpl` and available via file creating commands are in ~/.config/sherpa/templates. Copy there one or those that you need from here, and create your own ones.
